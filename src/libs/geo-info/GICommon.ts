// Enum
enum ELevelStr {
    POSI = 'po',
    TRI = '_t',
    VERT = '_v',
    EDGE = '_e',
    WIRE = '_w',
    FACE = '_f',
    POINT = 'pt',
    LINE = 'ls',
    PGON = 'pg',
    COLL = 'co'
}

/**
 * Enum of the 6 levels at which attribtes can be added.
 */
enum ELevels {
    POSIS,
    VERTS,
    EDGES,
    WIRES,
    FACES,
    COLLS
}
