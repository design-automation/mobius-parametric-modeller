export enum PortType {
    Input,
    Output
}

export enum InputType {
    SimpleInput,
    Slider,
    Checkbox,
    URL,
    File,
    Constant,
    Dropdown
}

export enum OutputType {
    Text,
    Code
}
